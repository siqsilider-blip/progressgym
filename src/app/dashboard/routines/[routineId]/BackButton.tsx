import DashboardBackButton from '@/components/dashboard/DashboardBackButton'

export default function BackButton() {
    return (
        <DashboardBackButton fallbackHref="/dashboard/routines" iconOnly />
    )
}
